apply plugin: 'java'

group = 'net.xeraa'
version = '1.0-SNAPSHOT'

description = """Morphia Demo"""

sourceCompatibility = 1.7
targetCompatibility = 1.7

repositories {
    mavenCentral()
    mavenLocal()
}
dependencies {
    compile 'org.mongodb:mongo-java-driver:2.12.4',
            'org.mongodb.morphia:morphia:0.108',
            'org.springframework.security:spring-security-config:3.2.5.RELEASE' // Encryption and hashing
    testCompile 'junit:junit:4.11',
                'de.flapdoodle.embed:de.flapdoodle.embed.mongo:1.46.1' // Embedded MongoDB
}
